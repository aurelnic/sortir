{% extends 'base.html.twig' %}

{%  block title %}
    {{ parent() }} | Gestion Villes
{% endblock %}

{% block body %}

<h2>Gérer les villes</h2>

<div id="filtrage">
    <h3>Filtrer les villes</h3>
    {{ form_start(filtreForm)}}
    Le nom contient : {{ form_widget(filtreForm.nom)}} <button class="button">Rechercher</button>
    {{ form_end(filtreForm) }}
</div>


<table class="hover">
    <tbody>
        <tr>
            <td>Ville</td>
            <td>Code Postal</td>
            <td>Actions</td>
        </tr>

        {% for ville in villes %}
            <tr>
            <td>{{ ville.nomVille }}</td>
            <td>{{ ville.codePostal }}</td>
            <td><a href="{{ path('gestion_villes_edit', {'id': ville.id}) }}" class="button">Modifier</a>   <a href="{{ path('gestion_villes_delete', {'id': ville.id}) }}" class="button">Supprimer</a></td>
            </tr>
        {% endfor %}

        <tr>
        {{ form_start(villeajoutform) }}
        <td>{{ form_widget(villeajoutform.nomVille) }}</td>
        <td>{{ form_widget(villeajoutform.codePostal) }}</td>
        <td><button class="button">Ajouter</button></td>
        {{ form_end(villeajoutform)}}
        </tr>
    </tbody>
</table>
{% endblock %}


