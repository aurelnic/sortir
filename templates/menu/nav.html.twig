<nav>
    <ul>
        <li><a href="{{ path('gestion_villes') }}" title="Villes">Villes</a></li>
        <li><a href="{{ path('gestion_sites') }}" title="Sites">Sites</a></li>

        <li><a href="{{ path('main_index', {'_locale': 'fr'}) }}" title="Index">Accueil</a></li>
        {% if app.user %}
            <li><a href="{{ path('app_detail_profil', {'id': app.user.id}) }}" title="Profil">Mon profil</a></li>
            <li><a href="{{ path('app_logout') }}" title="Se déconnecter">Se déconnecter</a></li>
        {% if is_granted('ROLE_ADMIN') %}

        {% endif %}
        {% else %}
            <li><a href="{{ path('app_creation_profil') }}" title="S'inscrire">S'inscrire</a></li>
            <li><a href="{{ path('app_login') }}" title="Se connecter">Se connecter</a></li>
         {% endif %}
    </ul>
</nav>

{# <nav>
    <ul>
        <li><a href="{{ path('main_index', {'_locale': 'fr'}) }}" title="Index">Accueil</a></li>

        {% if app.user %}
            <li><a href="{{ path('app_logout') }}" title="Logout">Se déconnecter ({{ app.user.firstname }})</a></li>
            {% if is_granted('ROLE_ADMIN') %}
                <li><a href="{{ path('admin_dashboard') }}" title="Villes">Villes</a></li>
                <li><a href="{{ path('admin_dashboard') }}" title="Sites">Sites</a></li>
            {% endif %}
        {% else %}
            <li><a href="{{ path('app_register') }}" title="Register">S'inscrire</a></li>
            <li><a href="{{ path('app_login') }}" title="Login">Se connecter</a></li>
        {% endif %}
    </ul>
</nav> #}